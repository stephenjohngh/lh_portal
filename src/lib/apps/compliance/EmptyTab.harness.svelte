<!-- Test harness only (see CLAUDE.md: *.harness.svelte are not runtime
     components). Stands in for a Compliance tab in ComplianceApp.test.js, so
     the shell can be rendered without mocking every store the tabs load. -->
<p data-testid="compliance-tab">tab</p>
