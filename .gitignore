node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env — cover both .env.* names and anything ending in .env
# (e.g. stray copies like "service Copy.env" that hold live keys)
.env
.env.*
*.env
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.claude/


cmds.txt

# DB backups (contains credentials / large sql dumps)
# Use /scripts/* (not /scripts/) so individual tooling files can be re-included.
/scripts/*
/backups/
# …but track committed dev tooling that lives under scripts/ (no secrets)
!/scripts/gen-db-types.mjs
